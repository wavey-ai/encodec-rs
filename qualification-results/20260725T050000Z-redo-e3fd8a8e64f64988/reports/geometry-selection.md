# Geometry Selection

Status: blocked.

The run measures the current unpublished `acv=2` envelope. It does not measure a complete Profile 1 object.

The Meta candidate is blocked because the current ONNX runner pads its final segment. This does not prove the required true variable-tail rule.

The fixed candidates can provide provisional CPU measurements. They cannot lock a geometry while the worktree is dirty and fixed-code evidence is incomplete.
