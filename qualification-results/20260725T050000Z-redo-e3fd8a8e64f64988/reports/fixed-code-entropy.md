# Fixed-Code and Entropy Evidence

Status: blocked.

The LM probe can record the 203-step CDF digest. The current CLI does not export model input tensors, recovered code tensors, codebook order vectors, scale bits, or entropy bytes.
