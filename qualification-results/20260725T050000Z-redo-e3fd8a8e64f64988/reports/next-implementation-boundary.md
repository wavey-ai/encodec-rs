# Next Implementation Boundary

Stop before wire-format freeze.

First add provider-neutral model-input, codes, recovered-codes, scale, and entropy evidence. Then add true variable-tail execution for Meta.

After the owner approves one geometry, implement Profile 1 encoding and strict and salvage decoding.
